import { StyleSheet } from "react-native";
import { colors } from "../../constants/colors";

export default ordersStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: colors.grey,
  },
});
