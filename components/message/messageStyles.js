import { StyleSheet } from "react-native";

const messageStyles = StyleSheet.create({
  message: {
    text: { fontSize: 15, color: "#DB405C", fontWeight: 500, marginBottom: 5 },
  },
});

export default messageStyles;
